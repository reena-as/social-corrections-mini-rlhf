#!/usr/bin/env python
"""Merge all correction-pair sources (seed + PoliteRewrite + SOTOPIA) with dedupe."""
from social_corrections.data.merge_datasets import main

if __name__ == "__main__":
    main()
